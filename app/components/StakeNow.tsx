'use client'
import React from "react";

const StakeNow = () => {
  return (
    <button className="btn btn-sm btn-primary" onClick={() => alert("hello")}>
      Stake Now!
    </button>
  );
};

export default StakeNow;
